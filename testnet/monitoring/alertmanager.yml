global:
  resolve_timeout: 5m

route:
  receiver: "webhook-log"
  group_by: ["alertname", "instance"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: "webhook-log"
    webhook_configs:
      - url: "http://localhost:5001/"
        send_resolved: true
