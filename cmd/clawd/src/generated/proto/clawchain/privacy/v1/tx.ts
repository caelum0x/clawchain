// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/privacy/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params.js";

export const protobufPackage = "clawchain.privacy.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /** NOTE: All parameters must be supplied. */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/** MsgPrivateTransfer defines the MsgPrivateTransfer message. */
export interface MsgPrivateTransfer {
  creator: string;
  oldCommitments: string;
  newCommitments: string;
  nullifiers: string;
  root: string;
  proof: string;
}

/** MsgPrivateTransferResponse defines the MsgPrivateTransferResponse message. */
export interface MsgPrivateTransferResponse {
}

/** MsgShield defines the MsgShield message. */
export interface MsgShield {
  creator: string;
  amount: string;
  coins: string;
  /**
   * blinding is a 32-byte cryptographic random blinding factor provided by the client.
   * This MUST be generated client-side using a CSPRNG. Empty blinding is rejected.
   */
  blinding: Uint8Array;
}

/** MsgShieldResponse defines the MsgShieldResponse message. */
export interface MsgShieldResponse {
}

/** MsgUnshield defines the MsgUnshield message. */
export interface MsgUnshield {
  creator: string;
  commitment: string;
  nullifier: string;
  proof: string;
  amount: string;
  recipient: string;
  root: string;
}

/** MsgUnshieldResponse defines the MsgUnshieldResponse message. */
export interface MsgUnshieldResponse {
}

/** MsgRegisterViewKey defines the message for registering an encrypted note view key. */
export interface MsgRegisterViewKey {
  creator: string;
  commitmentHex: string;
  encryptedNote: string;
}

/** MsgRegisterViewKeyResponse defines the response for MsgRegisterViewKey. */
export interface MsgRegisterViewKeyResponse {
}

/** BatchTransferEntry represents a single transfer inside a batch submission. */
export interface BatchTransferEntry {
  oldCommitments: string;
  newCommitments: string;
  nullifiers: string;
  root: string;
  proof: string;
}

/** MsgBatchPrivateTransfer defines the message for batched private transfers. */
export interface MsgBatchPrivateTransfer {
  creator: string;
  transfers: BatchTransferEntry[];
}

/** MsgBatchPrivateTransferResponse defines the response for MsgBatchPrivateTransfer. */
export interface MsgBatchPrivateTransferResponse {
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgPrivateTransfer(): MsgPrivateTransfer {
  return { creator: "", oldCommitments: "", newCommitments: "", nullifiers: "", root: "", proof: "" };
}

export const MsgPrivateTransfer: MessageFns<MsgPrivateTransfer> = {
  encode(message: MsgPrivateTransfer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.oldCommitments !== "") {
      writer.uint32(18).string(message.oldCommitments);
    }
    if (message.newCommitments !== "") {
      writer.uint32(26).string(message.newCommitments);
    }
    if (message.nullifiers !== "") {
      writer.uint32(34).string(message.nullifiers);
    }
    if (message.root !== "") {
      writer.uint32(42).string(message.root);
    }
    if (message.proof !== "") {
      writer.uint32(50).string(message.proof);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPrivateTransfer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPrivateTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.oldCommitments = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newCommitments = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.nullifiers = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.root = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.proof = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgPrivateTransfer {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      oldCommitments: isSet(object.oldCommitments)
        ? globalThis.String(object.oldCommitments)
        : isSet(object.old_commitments)
        ? globalThis.String(object.old_commitments)
        : "",
      newCommitments: isSet(object.newCommitments)
        ? globalThis.String(object.newCommitments)
        : isSet(object.new_commitments)
        ? globalThis.String(object.new_commitments)
        : "",
      nullifiers: isSet(object.nullifiers) ? globalThis.String(object.nullifiers) : "",
      root: isSet(object.root) ? globalThis.String(object.root) : "",
      proof: isSet(object.proof) ? globalThis.String(object.proof) : "",
    };
  },

  toJSON(message: MsgPrivateTransfer): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.oldCommitments !== "") {
      obj.oldCommitments = message.oldCommitments;
    }
    if (message.newCommitments !== "") {
      obj.newCommitments = message.newCommitments;
    }
    if (message.nullifiers !== "") {
      obj.nullifiers = message.nullifiers;
    }
    if (message.root !== "") {
      obj.root = message.root;
    }
    if (message.proof !== "") {
      obj.proof = message.proof;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgPrivateTransfer>): MsgPrivateTransfer {
    return MsgPrivateTransfer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgPrivateTransfer>): MsgPrivateTransfer {
    const message = createBaseMsgPrivateTransfer();
    message.creator = object.creator ?? "";
    message.oldCommitments = object.oldCommitments ?? "";
    message.newCommitments = object.newCommitments ?? "";
    message.nullifiers = object.nullifiers ?? "";
    message.root = object.root ?? "";
    message.proof = object.proof ?? "";
    return message;
  },
};

function createBaseMsgPrivateTransferResponse(): MsgPrivateTransferResponse {
  return {};
}

export const MsgPrivateTransferResponse: MessageFns<MsgPrivateTransferResponse> = {
  encode(_: MsgPrivateTransferResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgPrivateTransferResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgPrivateTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgPrivateTransferResponse {
    return {};
  },

  toJSON(_: MsgPrivateTransferResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgPrivateTransferResponse>): MsgPrivateTransferResponse {
    return MsgPrivateTransferResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgPrivateTransferResponse>): MsgPrivateTransferResponse {
    const message = createBaseMsgPrivateTransferResponse();
    return message;
  },
};

function createBaseMsgShield(): MsgShield {
  return { creator: "", amount: "0", coins: "", blinding: new Uint8Array(0) };
}

export const MsgShield: MessageFns<MsgShield> = {
  encode(message: MsgShield, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.amount !== "0") {
      writer.uint32(16).uint64(message.amount);
    }
    if (message.coins !== "") {
      writer.uint32(26).string(message.coins);
    }
    if (message.blinding.length !== 0) {
      writer.uint32(34).bytes(message.blinding);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgShield {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgShield();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.amount = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.coins = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.blinding = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgShield {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "0",
      coins: isSet(object.coins) ? globalThis.String(object.coins) : "",
      blinding: isSet(object.blinding) ? bytesFromBase64(object.blinding) : new Uint8Array(0),
    };
  },

  toJSON(message: MsgShield): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.amount !== "0") {
      obj.amount = message.amount;
    }
    if (message.coins !== "") {
      obj.coins = message.coins;
    }
    if (message.blinding.length !== 0) {
      obj.blinding = base64FromBytes(message.blinding);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgShield>): MsgShield {
    return MsgShield.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgShield>): MsgShield {
    const message = createBaseMsgShield();
    message.creator = object.creator ?? "";
    message.amount = object.amount ?? "0";
    message.coins = object.coins ?? "";
    message.blinding = object.blinding ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgShieldResponse(): MsgShieldResponse {
  return {};
}

export const MsgShieldResponse: MessageFns<MsgShieldResponse> = {
  encode(_: MsgShieldResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgShieldResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgShieldResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgShieldResponse {
    return {};
  },

  toJSON(_: MsgShieldResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgShieldResponse>): MsgShieldResponse {
    return MsgShieldResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgShieldResponse>): MsgShieldResponse {
    const message = createBaseMsgShieldResponse();
    return message;
  },
};

function createBaseMsgUnshield(): MsgUnshield {
  return { creator: "", commitment: "", nullifier: "", proof: "", amount: "0", recipient: "", root: "" };
}

export const MsgUnshield: MessageFns<MsgUnshield> = {
  encode(message: MsgUnshield, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.commitment !== "") {
      writer.uint32(18).string(message.commitment);
    }
    if (message.nullifier !== "") {
      writer.uint32(26).string(message.nullifier);
    }
    if (message.proof !== "") {
      writer.uint32(34).string(message.proof);
    }
    if (message.amount !== "0") {
      writer.uint32(40).uint64(message.amount);
    }
    if (message.recipient !== "") {
      writer.uint32(50).string(message.recipient);
    }
    if (message.root !== "") {
      writer.uint32(58).string(message.root);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUnshield {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUnshield();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commitment = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nullifier = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.proof = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.amount = reader.uint64().toString();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.recipient = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.root = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUnshield {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      commitment: isSet(object.commitment) ? globalThis.String(object.commitment) : "",
      nullifier: isSet(object.nullifier) ? globalThis.String(object.nullifier) : "",
      proof: isSet(object.proof) ? globalThis.String(object.proof) : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "0",
      recipient: isSet(object.recipient) ? globalThis.String(object.recipient) : "",
      root: isSet(object.root) ? globalThis.String(object.root) : "",
    };
  },

  toJSON(message: MsgUnshield): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.commitment !== "") {
      obj.commitment = message.commitment;
    }
    if (message.nullifier !== "") {
      obj.nullifier = message.nullifier;
    }
    if (message.proof !== "") {
      obj.proof = message.proof;
    }
    if (message.amount !== "0") {
      obj.amount = message.amount;
    }
    if (message.recipient !== "") {
      obj.recipient = message.recipient;
    }
    if (message.root !== "") {
      obj.root = message.root;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUnshield>): MsgUnshield {
    return MsgUnshield.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUnshield>): MsgUnshield {
    const message = createBaseMsgUnshield();
    message.creator = object.creator ?? "";
    message.commitment = object.commitment ?? "";
    message.nullifier = object.nullifier ?? "";
    message.proof = object.proof ?? "";
    message.amount = object.amount ?? "0";
    message.recipient = object.recipient ?? "";
    message.root = object.root ?? "";
    return message;
  },
};

function createBaseMsgUnshieldResponse(): MsgUnshieldResponse {
  return {};
}

export const MsgUnshieldResponse: MessageFns<MsgUnshieldResponse> = {
  encode(_: MsgUnshieldResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUnshieldResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUnshieldResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUnshieldResponse {
    return {};
  },

  toJSON(_: MsgUnshieldResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUnshieldResponse>): MsgUnshieldResponse {
    return MsgUnshieldResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUnshieldResponse>): MsgUnshieldResponse {
    const message = createBaseMsgUnshieldResponse();
    return message;
  },
};

function createBaseMsgRegisterViewKey(): MsgRegisterViewKey {
  return { creator: "", commitmentHex: "", encryptedNote: "" };
}

export const MsgRegisterViewKey: MessageFns<MsgRegisterViewKey> = {
  encode(message: MsgRegisterViewKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.commitmentHex !== "") {
      writer.uint32(18).string(message.commitmentHex);
    }
    if (message.encryptedNote !== "") {
      writer.uint32(26).string(message.encryptedNote);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterViewKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterViewKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commitmentHex = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.encryptedNote = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRegisterViewKey {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      commitmentHex: isSet(object.commitmentHex)
        ? globalThis.String(object.commitmentHex)
        : isSet(object.commitment_hex)
        ? globalThis.String(object.commitment_hex)
        : "",
      encryptedNote: isSet(object.encryptedNote)
        ? globalThis.String(object.encryptedNote)
        : isSet(object.encrypted_note)
        ? globalThis.String(object.encrypted_note)
        : "",
    };
  },

  toJSON(message: MsgRegisterViewKey): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.commitmentHex !== "") {
      obj.commitmentHex = message.commitmentHex;
    }
    if (message.encryptedNote !== "") {
      obj.encryptedNote = message.encryptedNote;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterViewKey>): MsgRegisterViewKey {
    return MsgRegisterViewKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRegisterViewKey>): MsgRegisterViewKey {
    const message = createBaseMsgRegisterViewKey();
    message.creator = object.creator ?? "";
    message.commitmentHex = object.commitmentHex ?? "";
    message.encryptedNote = object.encryptedNote ?? "";
    return message;
  },
};

function createBaseMsgRegisterViewKeyResponse(): MsgRegisterViewKeyResponse {
  return {};
}

export const MsgRegisterViewKeyResponse: MessageFns<MsgRegisterViewKeyResponse> = {
  encode(_: MsgRegisterViewKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRegisterViewKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRegisterViewKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRegisterViewKeyResponse {
    return {};
  },

  toJSON(_: MsgRegisterViewKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgRegisterViewKeyResponse>): MsgRegisterViewKeyResponse {
    return MsgRegisterViewKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgRegisterViewKeyResponse>): MsgRegisterViewKeyResponse {
    const message = createBaseMsgRegisterViewKeyResponse();
    return message;
  },
};

function createBaseBatchTransferEntry(): BatchTransferEntry {
  return { oldCommitments: "", newCommitments: "", nullifiers: "", root: "", proof: "" };
}

export const BatchTransferEntry: MessageFns<BatchTransferEntry> = {
  encode(message: BatchTransferEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oldCommitments !== "") {
      writer.uint32(10).string(message.oldCommitments);
    }
    if (message.newCommitments !== "") {
      writer.uint32(18).string(message.newCommitments);
    }
    if (message.nullifiers !== "") {
      writer.uint32(26).string(message.nullifiers);
    }
    if (message.root !== "") {
      writer.uint32(34).string(message.root);
    }
    if (message.proof !== "") {
      writer.uint32(42).string(message.proof);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchTransferEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchTransferEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.oldCommitments = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.newCommitments = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.nullifiers = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.root = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.proof = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchTransferEntry {
    return {
      oldCommitments: isSet(object.oldCommitments)
        ? globalThis.String(object.oldCommitments)
        : isSet(object.old_commitments)
        ? globalThis.String(object.old_commitments)
        : "",
      newCommitments: isSet(object.newCommitments)
        ? globalThis.String(object.newCommitments)
        : isSet(object.new_commitments)
        ? globalThis.String(object.new_commitments)
        : "",
      nullifiers: isSet(object.nullifiers) ? globalThis.String(object.nullifiers) : "",
      root: isSet(object.root) ? globalThis.String(object.root) : "",
      proof: isSet(object.proof) ? globalThis.String(object.proof) : "",
    };
  },

  toJSON(message: BatchTransferEntry): unknown {
    const obj: any = {};
    if (message.oldCommitments !== "") {
      obj.oldCommitments = message.oldCommitments;
    }
    if (message.newCommitments !== "") {
      obj.newCommitments = message.newCommitments;
    }
    if (message.nullifiers !== "") {
      obj.nullifiers = message.nullifiers;
    }
    if (message.root !== "") {
      obj.root = message.root;
    }
    if (message.proof !== "") {
      obj.proof = message.proof;
    }
    return obj;
  },

  create(base?: DeepPartial<BatchTransferEntry>): BatchTransferEntry {
    return BatchTransferEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BatchTransferEntry>): BatchTransferEntry {
    const message = createBaseBatchTransferEntry();
    message.oldCommitments = object.oldCommitments ?? "";
    message.newCommitments = object.newCommitments ?? "";
    message.nullifiers = object.nullifiers ?? "";
    message.root = object.root ?? "";
    message.proof = object.proof ?? "";
    return message;
  },
};

function createBaseMsgBatchPrivateTransfer(): MsgBatchPrivateTransfer {
  return { creator: "", transfers: [] };
}

export const MsgBatchPrivateTransfer: MessageFns<MsgBatchPrivateTransfer> = {
  encode(message: MsgBatchPrivateTransfer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    for (const v of message.transfers) {
      BatchTransferEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBatchPrivateTransfer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBatchPrivateTransfer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.transfers.push(BatchTransferEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgBatchPrivateTransfer {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      transfers: globalThis.Array.isArray(object?.transfers)
        ? object.transfers.map((e: any) => BatchTransferEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MsgBatchPrivateTransfer): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.transfers?.length) {
      obj.transfers = message.transfers.map((e) => BatchTransferEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<MsgBatchPrivateTransfer>): MsgBatchPrivateTransfer {
    return MsgBatchPrivateTransfer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgBatchPrivateTransfer>): MsgBatchPrivateTransfer {
    const message = createBaseMsgBatchPrivateTransfer();
    message.creator = object.creator ?? "";
    message.transfers = object.transfers?.map((e) => BatchTransferEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMsgBatchPrivateTransferResponse(): MsgBatchPrivateTransferResponse {
  return {};
}

export const MsgBatchPrivateTransferResponse: MessageFns<MsgBatchPrivateTransferResponse> = {
  encode(_: MsgBatchPrivateTransferResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgBatchPrivateTransferResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgBatchPrivateTransferResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgBatchPrivateTransferResponse {
    return {};
  },

  toJSON(_: MsgBatchPrivateTransferResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgBatchPrivateTransferResponse>): MsgBatchPrivateTransferResponse {
    return MsgBatchPrivateTransferResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgBatchPrivateTransferResponse>): MsgBatchPrivateTransferResponse {
    const message = createBaseMsgBatchPrivateTransferResponse();
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
