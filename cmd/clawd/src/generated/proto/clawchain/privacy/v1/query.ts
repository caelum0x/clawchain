// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/privacy/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params.js";

export const protobufPackage = "clawchain.privacy.v1";

/** QueryParamsRequest is request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params holds all the parameters of this module. */
  params: Params | undefined;
}

/** QueryMerkleRootRequest defines the QueryMerkleRootRequest message. */
export interface QueryMerkleRootRequest {
}

/** QueryMerkleRootResponse defines the QueryMerkleRootResponse message. */
export interface QueryMerkleRootResponse {
  root: string;
}

/** QueryNullifierExistsRequest defines the QueryNullifierExistsRequest message. */
export interface QueryNullifierExistsRequest {
  nullifier: string;
}

/** QueryNullifierExistsResponse defines the QueryNullifierExistsResponse message. */
export interface QueryNullifierExistsResponse {
  exists: boolean;
}

export interface QueryRootHistoryRequest {
  offset: string;
  limit: string;
}

export interface QueryRootHistoryResponse {
  roots: string[];
  nextOffset: string;
  total: string;
}

export interface QueryViewKeyRequest {
  commitmentHex: string;
}

export interface QueryViewKeyResponse {
  encryptedNote: string;
  found: boolean;
}

export interface QueryVerifyAmountProofRequest {
  commitmentHex: string;
  amount: string;
  proof: Uint8Array;
}

export interface QueryVerifyAmountProofResponse {
  valid: boolean;
}

export interface QueryMerkleProofRequest {
  commitmentHex: string;
}

export interface QueryMerkleProofResponse {
  leafIndex: string;
  path: string[];
  indices: string[];
  root: string;
  found: boolean;
}

export interface QueryCommitmentIndexRequest {
  commitmentHex: string;
}

export interface QueryCommitmentIndexResponse {
  leafIndex: string;
  found: boolean;
}

export interface QueryTreeStatsRequest {
}

export interface QueryTreeStatsResponse {
  leafCount: string;
  currentRoot: string;
  treeDepth: number;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryMerkleRootRequest(): QueryMerkleRootRequest {
  return {};
}

export const QueryMerkleRootRequest: MessageFns<QueryMerkleRootRequest> = {
  encode(_: QueryMerkleRootRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMerkleRootRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMerkleRootRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryMerkleRootRequest {
    return {};
  },

  toJSON(_: QueryMerkleRootRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryMerkleRootRequest>): QueryMerkleRootRequest {
    return QueryMerkleRootRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryMerkleRootRequest>): QueryMerkleRootRequest {
    const message = createBaseQueryMerkleRootRequest();
    return message;
  },
};

function createBaseQueryMerkleRootResponse(): QueryMerkleRootResponse {
  return { root: "" };
}

export const QueryMerkleRootResponse: MessageFns<QueryMerkleRootResponse> = {
  encode(message: QueryMerkleRootResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.root !== "") {
      writer.uint32(10).string(message.root);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMerkleRootResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMerkleRootResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.root = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMerkleRootResponse {
    return { root: isSet(object.root) ? globalThis.String(object.root) : "" };
  },

  toJSON(message: QueryMerkleRootResponse): unknown {
    const obj: any = {};
    if (message.root !== "") {
      obj.root = message.root;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMerkleRootResponse>): QueryMerkleRootResponse {
    return QueryMerkleRootResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMerkleRootResponse>): QueryMerkleRootResponse {
    const message = createBaseQueryMerkleRootResponse();
    message.root = object.root ?? "";
    return message;
  },
};

function createBaseQueryNullifierExistsRequest(): QueryNullifierExistsRequest {
  return { nullifier: "" };
}

export const QueryNullifierExistsRequest: MessageFns<QueryNullifierExistsRequest> = {
  encode(message: QueryNullifierExistsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nullifier !== "") {
      writer.uint32(10).string(message.nullifier);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryNullifierExistsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryNullifierExistsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nullifier = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryNullifierExistsRequest {
    return { nullifier: isSet(object.nullifier) ? globalThis.String(object.nullifier) : "" };
  },

  toJSON(message: QueryNullifierExistsRequest): unknown {
    const obj: any = {};
    if (message.nullifier !== "") {
      obj.nullifier = message.nullifier;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryNullifierExistsRequest>): QueryNullifierExistsRequest {
    return QueryNullifierExistsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryNullifierExistsRequest>): QueryNullifierExistsRequest {
    const message = createBaseQueryNullifierExistsRequest();
    message.nullifier = object.nullifier ?? "";
    return message;
  },
};

function createBaseQueryNullifierExistsResponse(): QueryNullifierExistsResponse {
  return { exists: false };
}

export const QueryNullifierExistsResponse: MessageFns<QueryNullifierExistsResponse> = {
  encode(message: QueryNullifierExistsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exists !== false) {
      writer.uint32(8).bool(message.exists);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryNullifierExistsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryNullifierExistsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exists = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryNullifierExistsResponse {
    return { exists: isSet(object.exists) ? globalThis.Boolean(object.exists) : false };
  },

  toJSON(message: QueryNullifierExistsResponse): unknown {
    const obj: any = {};
    if (message.exists !== false) {
      obj.exists = message.exists;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryNullifierExistsResponse>): QueryNullifierExistsResponse {
    return QueryNullifierExistsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryNullifierExistsResponse>): QueryNullifierExistsResponse {
    const message = createBaseQueryNullifierExistsResponse();
    message.exists = object.exists ?? false;
    return message;
  },
};

function createBaseQueryRootHistoryRequest(): QueryRootHistoryRequest {
  return { offset: "0", limit: "0" };
}

export const QueryRootHistoryRequest: MessageFns<QueryRootHistoryRequest> = {
  encode(message: QueryRootHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.offset !== "0") {
      writer.uint32(8).uint64(message.offset);
    }
    if (message.limit !== "0") {
      writer.uint32(16).uint64(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRootHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRootHistoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.offset = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRootHistoryRequest {
    return {
      offset: isSet(object.offset) ? globalThis.String(object.offset) : "0",
      limit: isSet(object.limit) ? globalThis.String(object.limit) : "0",
    };
  },

  toJSON(message: QueryRootHistoryRequest): unknown {
    const obj: any = {};
    if (message.offset !== "0") {
      obj.offset = message.offset;
    }
    if (message.limit !== "0") {
      obj.limit = message.limit;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRootHistoryRequest>): QueryRootHistoryRequest {
    return QueryRootHistoryRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRootHistoryRequest>): QueryRootHistoryRequest {
    const message = createBaseQueryRootHistoryRequest();
    message.offset = object.offset ?? "0";
    message.limit = object.limit ?? "0";
    return message;
  },
};

function createBaseQueryRootHistoryResponse(): QueryRootHistoryResponse {
  return { roots: [], nextOffset: "0", total: "0" };
}

export const QueryRootHistoryResponse: MessageFns<QueryRootHistoryResponse> = {
  encode(message: QueryRootHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.roots) {
      writer.uint32(10).string(v!);
    }
    if (message.nextOffset !== "0") {
      writer.uint32(16).uint64(message.nextOffset);
    }
    if (message.total !== "0") {
      writer.uint32(24).uint64(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRootHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryRootHistoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roots.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.nextOffset = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.total = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryRootHistoryResponse {
    return {
      roots: globalThis.Array.isArray(object?.roots) ? object.roots.map((e: any) => globalThis.String(e)) : [],
      nextOffset: isSet(object.nextOffset)
        ? globalThis.String(object.nextOffset)
        : isSet(object.next_offset)
        ? globalThis.String(object.next_offset)
        : "0",
      total: isSet(object.total) ? globalThis.String(object.total) : "0",
    };
  },

  toJSON(message: QueryRootHistoryResponse): unknown {
    const obj: any = {};
    if (message.roots?.length) {
      obj.roots = message.roots;
    }
    if (message.nextOffset !== "0") {
      obj.nextOffset = message.nextOffset;
    }
    if (message.total !== "0") {
      obj.total = message.total;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryRootHistoryResponse>): QueryRootHistoryResponse {
    return QueryRootHistoryResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryRootHistoryResponse>): QueryRootHistoryResponse {
    const message = createBaseQueryRootHistoryResponse();
    message.roots = object.roots?.map((e) => e) || [];
    message.nextOffset = object.nextOffset ?? "0";
    message.total = object.total ?? "0";
    return message;
  },
};

function createBaseQueryViewKeyRequest(): QueryViewKeyRequest {
  return { commitmentHex: "" };
}

export const QueryViewKeyRequest: MessageFns<QueryViewKeyRequest> = {
  encode(message: QueryViewKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commitmentHex !== "") {
      writer.uint32(10).string(message.commitmentHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryViewKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryViewKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitmentHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryViewKeyRequest {
    return {
      commitmentHex: isSet(object.commitmentHex)
        ? globalThis.String(object.commitmentHex)
        : isSet(object.commitment_hex)
        ? globalThis.String(object.commitment_hex)
        : "",
    };
  },

  toJSON(message: QueryViewKeyRequest): unknown {
    const obj: any = {};
    if (message.commitmentHex !== "") {
      obj.commitmentHex = message.commitmentHex;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryViewKeyRequest>): QueryViewKeyRequest {
    return QueryViewKeyRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryViewKeyRequest>): QueryViewKeyRequest {
    const message = createBaseQueryViewKeyRequest();
    message.commitmentHex = object.commitmentHex ?? "";
    return message;
  },
};

function createBaseQueryViewKeyResponse(): QueryViewKeyResponse {
  return { encryptedNote: "", found: false };
}

export const QueryViewKeyResponse: MessageFns<QueryViewKeyResponse> = {
  encode(message: QueryViewKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.encryptedNote !== "") {
      writer.uint32(10).string(message.encryptedNote);
    }
    if (message.found !== false) {
      writer.uint32(16).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryViewKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryViewKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.encryptedNote = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryViewKeyResponse {
    return {
      encryptedNote: isSet(object.encryptedNote)
        ? globalThis.String(object.encryptedNote)
        : isSet(object.encrypted_note)
        ? globalThis.String(object.encrypted_note)
        : "",
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryViewKeyResponse): unknown {
    const obj: any = {};
    if (message.encryptedNote !== "") {
      obj.encryptedNote = message.encryptedNote;
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryViewKeyResponse>): QueryViewKeyResponse {
    return QueryViewKeyResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryViewKeyResponse>): QueryViewKeyResponse {
    const message = createBaseQueryViewKeyResponse();
    message.encryptedNote = object.encryptedNote ?? "";
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseQueryVerifyAmountProofRequest(): QueryVerifyAmountProofRequest {
  return { commitmentHex: "", amount: "0", proof: new Uint8Array(0) };
}

export const QueryVerifyAmountProofRequest: MessageFns<QueryVerifyAmountProofRequest> = {
  encode(message: QueryVerifyAmountProofRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commitmentHex !== "") {
      writer.uint32(10).string(message.commitmentHex);
    }
    if (message.amount !== "0") {
      writer.uint32(16).uint64(message.amount);
    }
    if (message.proof.length !== 0) {
      writer.uint32(26).bytes(message.proof);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVerifyAmountProofRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVerifyAmountProofRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitmentHex = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.amount = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.proof = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVerifyAmountProofRequest {
    return {
      commitmentHex: isSet(object.commitmentHex)
        ? globalThis.String(object.commitmentHex)
        : isSet(object.commitment_hex)
        ? globalThis.String(object.commitment_hex)
        : "",
      amount: isSet(object.amount) ? globalThis.String(object.amount) : "0",
      proof: isSet(object.proof) ? bytesFromBase64(object.proof) : new Uint8Array(0),
    };
  },

  toJSON(message: QueryVerifyAmountProofRequest): unknown {
    const obj: any = {};
    if (message.commitmentHex !== "") {
      obj.commitmentHex = message.commitmentHex;
    }
    if (message.amount !== "0") {
      obj.amount = message.amount;
    }
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryVerifyAmountProofRequest>): QueryVerifyAmountProofRequest {
    return QueryVerifyAmountProofRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryVerifyAmountProofRequest>): QueryVerifyAmountProofRequest {
    const message = createBaseQueryVerifyAmountProofRequest();
    message.commitmentHex = object.commitmentHex ?? "";
    message.amount = object.amount ?? "0";
    message.proof = object.proof ?? new Uint8Array(0);
    return message;
  },
};

function createBaseQueryVerifyAmountProofResponse(): QueryVerifyAmountProofResponse {
  return { valid: false };
}

export const QueryVerifyAmountProofResponse: MessageFns<QueryVerifyAmountProofResponse> = {
  encode(message: QueryVerifyAmountProofResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.valid !== false) {
      writer.uint32(8).bool(message.valid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVerifyAmountProofResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVerifyAmountProofResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.valid = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVerifyAmountProofResponse {
    return { valid: isSet(object.valid) ? globalThis.Boolean(object.valid) : false };
  },

  toJSON(message: QueryVerifyAmountProofResponse): unknown {
    const obj: any = {};
    if (message.valid !== false) {
      obj.valid = message.valid;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryVerifyAmountProofResponse>): QueryVerifyAmountProofResponse {
    return QueryVerifyAmountProofResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryVerifyAmountProofResponse>): QueryVerifyAmountProofResponse {
    const message = createBaseQueryVerifyAmountProofResponse();
    message.valid = object.valid ?? false;
    return message;
  },
};

function createBaseQueryMerkleProofRequest(): QueryMerkleProofRequest {
  return { commitmentHex: "" };
}

export const QueryMerkleProofRequest: MessageFns<QueryMerkleProofRequest> = {
  encode(message: QueryMerkleProofRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commitmentHex !== "") {
      writer.uint32(10).string(message.commitmentHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMerkleProofRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMerkleProofRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitmentHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMerkleProofRequest {
    return {
      commitmentHex: isSet(object.commitmentHex)
        ? globalThis.String(object.commitmentHex)
        : isSet(object.commitment_hex)
        ? globalThis.String(object.commitment_hex)
        : "",
    };
  },

  toJSON(message: QueryMerkleProofRequest): unknown {
    const obj: any = {};
    if (message.commitmentHex !== "") {
      obj.commitmentHex = message.commitmentHex;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMerkleProofRequest>): QueryMerkleProofRequest {
    return QueryMerkleProofRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMerkleProofRequest>): QueryMerkleProofRequest {
    const message = createBaseQueryMerkleProofRequest();
    message.commitmentHex = object.commitmentHex ?? "";
    return message;
  },
};

function createBaseQueryMerkleProofResponse(): QueryMerkleProofResponse {
  return { leafIndex: "0", path: [], indices: [], root: "", found: false };
}

export const QueryMerkleProofResponse: MessageFns<QueryMerkleProofResponse> = {
  encode(message: QueryMerkleProofResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leafIndex !== "0") {
      writer.uint32(8).uint64(message.leafIndex);
    }
    for (const v of message.path) {
      writer.uint32(18).string(v!);
    }
    writer.uint32(26).fork();
    for (const v of message.indices) {
      writer.uint64(v);
    }
    writer.join();
    if (message.root !== "") {
      writer.uint32(34).string(message.root);
    }
    if (message.found !== false) {
      writer.uint32(40).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMerkleProofResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMerkleProofResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.leafIndex = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path.push(reader.string());
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.indices.push(reader.uint64().toString());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.indices.push(reader.uint64().toString());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.root = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMerkleProofResponse {
    return {
      leafIndex: isSet(object.leafIndex)
        ? globalThis.String(object.leafIndex)
        : isSet(object.leaf_index)
        ? globalThis.String(object.leaf_index)
        : "0",
      path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => globalThis.String(e)) : [],
      indices: globalThis.Array.isArray(object?.indices) ? object.indices.map((e: any) => globalThis.String(e)) : [],
      root: isSet(object.root) ? globalThis.String(object.root) : "",
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryMerkleProofResponse): unknown {
    const obj: any = {};
    if (message.leafIndex !== "0") {
      obj.leafIndex = message.leafIndex;
    }
    if (message.path?.length) {
      obj.path = message.path;
    }
    if (message.indices?.length) {
      obj.indices = message.indices;
    }
    if (message.root !== "") {
      obj.root = message.root;
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMerkleProofResponse>): QueryMerkleProofResponse {
    return QueryMerkleProofResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMerkleProofResponse>): QueryMerkleProofResponse {
    const message = createBaseQueryMerkleProofResponse();
    message.leafIndex = object.leafIndex ?? "0";
    message.path = object.path?.map((e) => e) || [];
    message.indices = object.indices?.map((e) => e) || [];
    message.root = object.root ?? "";
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseQueryCommitmentIndexRequest(): QueryCommitmentIndexRequest {
  return { commitmentHex: "" };
}

export const QueryCommitmentIndexRequest: MessageFns<QueryCommitmentIndexRequest> = {
  encode(message: QueryCommitmentIndexRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commitmentHex !== "") {
      writer.uint32(10).string(message.commitmentHex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCommitmentIndexRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCommitmentIndexRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitmentHex = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCommitmentIndexRequest {
    return {
      commitmentHex: isSet(object.commitmentHex)
        ? globalThis.String(object.commitmentHex)
        : isSet(object.commitment_hex)
        ? globalThis.String(object.commitment_hex)
        : "",
    };
  },

  toJSON(message: QueryCommitmentIndexRequest): unknown {
    const obj: any = {};
    if (message.commitmentHex !== "") {
      obj.commitmentHex = message.commitmentHex;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryCommitmentIndexRequest>): QueryCommitmentIndexRequest {
    return QueryCommitmentIndexRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryCommitmentIndexRequest>): QueryCommitmentIndexRequest {
    const message = createBaseQueryCommitmentIndexRequest();
    message.commitmentHex = object.commitmentHex ?? "";
    return message;
  },
};

function createBaseQueryCommitmentIndexResponse(): QueryCommitmentIndexResponse {
  return { leafIndex: "0", found: false };
}

export const QueryCommitmentIndexResponse: MessageFns<QueryCommitmentIndexResponse> = {
  encode(message: QueryCommitmentIndexResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leafIndex !== "0") {
      writer.uint32(8).uint64(message.leafIndex);
    }
    if (message.found !== false) {
      writer.uint32(16).bool(message.found);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryCommitmentIndexResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryCommitmentIndexResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.leafIndex = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.found = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryCommitmentIndexResponse {
    return {
      leafIndex: isSet(object.leafIndex)
        ? globalThis.String(object.leafIndex)
        : isSet(object.leaf_index)
        ? globalThis.String(object.leaf_index)
        : "0",
      found: isSet(object.found) ? globalThis.Boolean(object.found) : false,
    };
  },

  toJSON(message: QueryCommitmentIndexResponse): unknown {
    const obj: any = {};
    if (message.leafIndex !== "0") {
      obj.leafIndex = message.leafIndex;
    }
    if (message.found !== false) {
      obj.found = message.found;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryCommitmentIndexResponse>): QueryCommitmentIndexResponse {
    return QueryCommitmentIndexResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryCommitmentIndexResponse>): QueryCommitmentIndexResponse {
    const message = createBaseQueryCommitmentIndexResponse();
    message.leafIndex = object.leafIndex ?? "0";
    message.found = object.found ?? false;
    return message;
  },
};

function createBaseQueryTreeStatsRequest(): QueryTreeStatsRequest {
  return {};
}

export const QueryTreeStatsRequest: MessageFns<QueryTreeStatsRequest> = {
  encode(_: QueryTreeStatsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryTreeStatsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryTreeStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryTreeStatsRequest {
    return {};
  },

  toJSON(_: QueryTreeStatsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryTreeStatsRequest>): QueryTreeStatsRequest {
    return QueryTreeStatsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryTreeStatsRequest>): QueryTreeStatsRequest {
    const message = createBaseQueryTreeStatsRequest();
    return message;
  },
};

function createBaseQueryTreeStatsResponse(): QueryTreeStatsResponse {
  return { leafCount: "0", currentRoot: "", treeDepth: 0 };
}

export const QueryTreeStatsResponse: MessageFns<QueryTreeStatsResponse> = {
  encode(message: QueryTreeStatsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.leafCount !== "0") {
      writer.uint32(8).uint64(message.leafCount);
    }
    if (message.currentRoot !== "") {
      writer.uint32(18).string(message.currentRoot);
    }
    if (message.treeDepth !== 0) {
      writer.uint32(24).uint32(message.treeDepth);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryTreeStatsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryTreeStatsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.leafCount = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.currentRoot = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.treeDepth = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryTreeStatsResponse {
    return {
      leafCount: isSet(object.leafCount)
        ? globalThis.String(object.leafCount)
        : isSet(object.leaf_count)
        ? globalThis.String(object.leaf_count)
        : "0",
      currentRoot: isSet(object.currentRoot)
        ? globalThis.String(object.currentRoot)
        : isSet(object.current_root)
        ? globalThis.String(object.current_root)
        : "",
      treeDepth: isSet(object.treeDepth)
        ? globalThis.Number(object.treeDepth)
        : isSet(object.tree_depth)
        ? globalThis.Number(object.tree_depth)
        : 0,
    };
  },

  toJSON(message: QueryTreeStatsResponse): unknown {
    const obj: any = {};
    if (message.leafCount !== "0") {
      obj.leafCount = message.leafCount;
    }
    if (message.currentRoot !== "") {
      obj.currentRoot = message.currentRoot;
    }
    if (message.treeDepth !== 0) {
      obj.treeDepth = Math.round(message.treeDepth);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryTreeStatsResponse>): QueryTreeStatsResponse {
    return QueryTreeStatsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryTreeStatsResponse>): QueryTreeStatsResponse {
    const message = createBaseQueryTreeStatsResponse();
    message.leafCount = object.leafCount ?? "0";
    message.currentRoot = object.currentRoot ?? "";
    message.treeDepth = object.treeDepth ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
