// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/marketplace/v1/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "clawchain.marketplace.v1";

/** SkillVersionEntry represents a versioned skill entry. */
export interface SkillVersionEntry {
  skillId: string;
  version: string;
  description: string;
  price: string;
  updatedAt: string;
  category: string;
  tags: string[];
  dependencies: string[];
}

function createBaseSkillVersionEntry(): SkillVersionEntry {
  return {
    skillId: "0",
    version: "0",
    description: "",
    price: "",
    updatedAt: "0",
    category: "",
    tags: [],
    dependencies: [],
  };
}

export const SkillVersionEntry: MessageFns<SkillVersionEntry> = {
  encode(message: SkillVersionEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.skillId !== "0") {
      writer.uint32(8).uint64(message.skillId);
    }
    if (message.version !== "0") {
      writer.uint32(16).uint64(message.version);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.price !== "") {
      writer.uint32(34).string(message.price);
    }
    if (message.updatedAt !== "0") {
      writer.uint32(40).int64(message.updatedAt);
    }
    if (message.category !== "") {
      writer.uint32(50).string(message.category);
    }
    for (const v of message.tags) {
      writer.uint32(58).string(v!);
    }
    writer.uint32(66).fork();
    for (const v of message.dependencies) {
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SkillVersionEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSkillVersionEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.skillId = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.version = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.price = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.updatedAt = reader.int64().toString();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tags.push(reader.string());
          continue;
        }
        case 8: {
          if (tag === 64) {
            message.dependencies.push(reader.uint64().toString());

            continue;
          }

          if (tag === 66) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.dependencies.push(reader.uint64().toString());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SkillVersionEntry {
    return {
      skillId: isSet(object.skillId)
        ? globalThis.String(object.skillId)
        : isSet(object.skill_id)
        ? globalThis.String(object.skill_id)
        : "0",
      version: isSet(object.version) ? globalThis.String(object.version) : "0",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      price: isSet(object.price) ? globalThis.String(object.price) : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "0",
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => globalThis.String(e)) : [],
      dependencies: globalThis.Array.isArray(object?.dependencies)
        ? object.dependencies.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SkillVersionEntry): unknown {
    const obj: any = {};
    if (message.skillId !== "0") {
      obj.skillId = message.skillId;
    }
    if (message.version !== "0") {
      obj.version = message.version;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.price !== "") {
      obj.price = message.price;
    }
    if (message.updatedAt !== "0") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.tags?.length) {
      obj.tags = message.tags;
    }
    if (message.dependencies?.length) {
      obj.dependencies = message.dependencies;
    }
    return obj;
  },

  create(base?: DeepPartial<SkillVersionEntry>): SkillVersionEntry {
    return SkillVersionEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SkillVersionEntry>): SkillVersionEntry {
    const message = createBaseSkillVersionEntry();
    message.skillId = object.skillId ?? "0";
    message.version = object.version ?? "0";
    message.description = object.description ?? "";
    message.price = object.price ?? "";
    message.updatedAt = object.updatedAt ?? "0";
    message.category = object.category ?? "";
    message.tags = object.tags?.map((e) => e) || [];
    message.dependencies = object.dependencies?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
