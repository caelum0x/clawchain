// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/governance/v1/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "clawchain.governance.v1";

/** Params defines the governance module parameters. */
export interface GovParams {
  /** voting_period_blocks is the number of blocks proposals remain open for voting. */
  votingPeriodBlocks: string;
  /** min_deposit_uclaw is the minimum deposit required to submit a proposal. */
  minDepositUclaw: string;
  /** quorum_bps is the minimum participation in basis points (e.g., 3300 = 33%). */
  quorumBps: string;
  /** threshold_bps is the yes vote percentage in basis points needed to pass (e.g., 5000 = 50%). */
  thresholdBps: string;
}

function createBaseGovParams(): GovParams {
  return { votingPeriodBlocks: "0", minDepositUclaw: "0", quorumBps: "0", thresholdBps: "0" };
}

export const GovParams: MessageFns<GovParams> = {
  encode(message: GovParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.votingPeriodBlocks !== "0") {
      writer.uint32(8).int64(message.votingPeriodBlocks);
    }
    if (message.minDepositUclaw !== "0") {
      writer.uint32(16).uint64(message.minDepositUclaw);
    }
    if (message.quorumBps !== "0") {
      writer.uint32(24).uint64(message.quorumBps);
    }
    if (message.thresholdBps !== "0") {
      writer.uint32(32).uint64(message.thresholdBps);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GovParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGovParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.votingPeriodBlocks = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minDepositUclaw = reader.uint64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.quorumBps = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.thresholdBps = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GovParams {
    return {
      votingPeriodBlocks: isSet(object.votingPeriodBlocks)
        ? globalThis.String(object.votingPeriodBlocks)
        : isSet(object.voting_period_blocks)
        ? globalThis.String(object.voting_period_blocks)
        : "0",
      minDepositUclaw: isSet(object.minDepositUclaw)
        ? globalThis.String(object.minDepositUclaw)
        : isSet(object.min_deposit_uclaw)
        ? globalThis.String(object.min_deposit_uclaw)
        : "0",
      quorumBps: isSet(object.quorumBps)
        ? globalThis.String(object.quorumBps)
        : isSet(object.quorum_bps)
        ? globalThis.String(object.quorum_bps)
        : "0",
      thresholdBps: isSet(object.thresholdBps)
        ? globalThis.String(object.thresholdBps)
        : isSet(object.threshold_bps)
        ? globalThis.String(object.threshold_bps)
        : "0",
    };
  },

  toJSON(message: GovParams): unknown {
    const obj: any = {};
    if (message.votingPeriodBlocks !== "0") {
      obj.votingPeriodBlocks = message.votingPeriodBlocks;
    }
    if (message.minDepositUclaw !== "0") {
      obj.minDepositUclaw = message.minDepositUclaw;
    }
    if (message.quorumBps !== "0") {
      obj.quorumBps = message.quorumBps;
    }
    if (message.thresholdBps !== "0") {
      obj.thresholdBps = message.thresholdBps;
    }
    return obj;
  },

  create(base?: DeepPartial<GovParams>): GovParams {
    return GovParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GovParams>): GovParams {
    const message = createBaseGovParams();
    message.votingPeriodBlocks = object.votingPeriodBlocks ?? "0";
    message.minDepositUclaw = object.minDepositUclaw ?? "0";
    message.quorumBps = object.quorumBps ?? "0";
    message.thresholdBps = object.thresholdBps ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
