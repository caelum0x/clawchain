// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/governance/v1/genesis.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GovParams } from "./params.js";
import { Proposal, Vote } from "./types.js";

export const protobufPackage = "clawchain.governance.v1";

/** GenesisState defines the governance module's genesis state. */
export interface GovGenesisState {
  params: GovParams | undefined;
  proposals: Proposal[];
  votes: Vote[];
  proposalCount: string;
}

function createBaseGovGenesisState(): GovGenesisState {
  return { params: undefined, proposals: [], votes: [], proposalCount: "0" };
}

export const GovGenesisState: MessageFns<GovGenesisState> = {
  encode(message: GovGenesisState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      GovParams.encode(message.params, writer.uint32(10).fork()).join();
    }
    for (const v of message.proposals) {
      Proposal.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.votes) {
      Vote.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.proposalCount !== "0") {
      writer.uint32(32).uint64(message.proposalCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GovGenesisState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGovGenesisState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = GovParams.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.proposals.push(Proposal.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.votes.push(Vote.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.proposalCount = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GovGenesisState {
    return {
      params: isSet(object.params) ? GovParams.fromJSON(object.params) : undefined,
      proposals: globalThis.Array.isArray(object?.proposals)
        ? object.proposals.map((e: any) => Proposal.fromJSON(e))
        : [],
      votes: globalThis.Array.isArray(object?.votes) ? object.votes.map((e: any) => Vote.fromJSON(e)) : [],
      proposalCount: isSet(object.proposalCount)
        ? globalThis.String(object.proposalCount)
        : isSet(object.proposal_count)
        ? globalThis.String(object.proposal_count)
        : "0",
    };
  },

  toJSON(message: GovGenesisState): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = GovParams.toJSON(message.params);
    }
    if (message.proposals?.length) {
      obj.proposals = message.proposals.map((e) => Proposal.toJSON(e));
    }
    if (message.votes?.length) {
      obj.votes = message.votes.map((e) => Vote.toJSON(e));
    }
    if (message.proposalCount !== "0") {
      obj.proposalCount = message.proposalCount;
    }
    return obj;
  },

  create(base?: DeepPartial<GovGenesisState>): GovGenesisState {
    return GovGenesisState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GovGenesisState>): GovGenesisState {
    const message = createBaseGovGenesisState();
    message.params = (object.params !== undefined && object.params !== null)
      ? GovParams.fromPartial(object.params)
      : undefined;
    message.proposals = object.proposals?.map((e) => Proposal.fromPartial(e)) || [];
    message.votes = object.votes?.map((e) => Vote.fromPartial(e)) || [];
    message.proposalCount = object.proposalCount ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
