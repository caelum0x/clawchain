// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/reputation/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params.js";

export const protobufPackage = "clawchain.reputation.v1";

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address that controls the module (defaults to x/gov unless overwritten). */
  authority: string;
  /**
   * params defines the module parameters to update.
   * NOTE: All parameters must be supplied.
   */
  params: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

/** MsgRateAgent defines the message for rating an agent. */
export interface MsgRateAgent {
  creator: string;
  agentAddress: string;
  skillId: string;
  score: number;
  comment: string;
}

/** MsgRateAgentResponse defines the response for MsgRateAgent. */
export interface MsgRateAgentResponse {
  ratingId: string;
}

/** MsgEndorseAgent defines the message for endorsing an agent. */
export interface MsgEndorseAgent {
  creator: string;
  agentAddress: string;
  reason: string;
}

/** MsgEndorseAgentResponse defines the response for MsgEndorseAgent. */
export interface MsgEndorseAgentResponse {
  endorsementId: string;
}

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgUpdateParams>): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<MsgUpdateParamsResponse>): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgRateAgent(): MsgRateAgent {
  return { creator: "", agentAddress: "", skillId: "0", score: 0, comment: "" };
}

export const MsgRateAgent: MessageFns<MsgRateAgent> = {
  encode(message: MsgRateAgent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.agentAddress !== "") {
      writer.uint32(18).string(message.agentAddress);
    }
    if (message.skillId !== "0") {
      writer.uint32(24).uint64(message.skillId);
    }
    if (message.score !== 0) {
      writer.uint32(32).uint32(message.score);
    }
    if (message.comment !== "") {
      writer.uint32(42).string(message.comment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRateAgent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRateAgent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.skillId = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.score = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.comment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRateAgent {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      agentAddress: isSet(object.agentAddress)
        ? globalThis.String(object.agentAddress)
        : isSet(object.agent_address)
        ? globalThis.String(object.agent_address)
        : "",
      skillId: isSet(object.skillId)
        ? globalThis.String(object.skillId)
        : isSet(object.skill_id)
        ? globalThis.String(object.skill_id)
        : "0",
      score: isSet(object.score) ? globalThis.Number(object.score) : 0,
      comment: isSet(object.comment) ? globalThis.String(object.comment) : "",
    };
  },

  toJSON(message: MsgRateAgent): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.agentAddress !== "") {
      obj.agentAddress = message.agentAddress;
    }
    if (message.skillId !== "0") {
      obj.skillId = message.skillId;
    }
    if (message.score !== 0) {
      obj.score = Math.round(message.score);
    }
    if (message.comment !== "") {
      obj.comment = message.comment;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRateAgent>): MsgRateAgent {
    return MsgRateAgent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRateAgent>): MsgRateAgent {
    const message = createBaseMsgRateAgent();
    message.creator = object.creator ?? "";
    message.agentAddress = object.agentAddress ?? "";
    message.skillId = object.skillId ?? "0";
    message.score = object.score ?? 0;
    message.comment = object.comment ?? "";
    return message;
  },
};

function createBaseMsgRateAgentResponse(): MsgRateAgentResponse {
  return { ratingId: "0" };
}

export const MsgRateAgentResponse: MessageFns<MsgRateAgentResponse> = {
  encode(message: MsgRateAgentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ratingId !== "0") {
      writer.uint32(8).uint64(message.ratingId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRateAgentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRateAgentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ratingId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRateAgentResponse {
    return {
      ratingId: isSet(object.ratingId)
        ? globalThis.String(object.ratingId)
        : isSet(object.rating_id)
        ? globalThis.String(object.rating_id)
        : "0",
    };
  },

  toJSON(message: MsgRateAgentResponse): unknown {
    const obj: any = {};
    if (message.ratingId !== "0") {
      obj.ratingId = message.ratingId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgRateAgentResponse>): MsgRateAgentResponse {
    return MsgRateAgentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgRateAgentResponse>): MsgRateAgentResponse {
    const message = createBaseMsgRateAgentResponse();
    message.ratingId = object.ratingId ?? "0";
    return message;
  },
};

function createBaseMsgEndorseAgent(): MsgEndorseAgent {
  return { creator: "", agentAddress: "", reason: "" };
}

export const MsgEndorseAgent: MessageFns<MsgEndorseAgent> = {
  encode(message: MsgEndorseAgent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.creator !== "") {
      writer.uint32(10).string(message.creator);
    }
    if (message.agentAddress !== "") {
      writer.uint32(18).string(message.agentAddress);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgEndorseAgent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgEndorseAgent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.creator = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.agentAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgEndorseAgent {
    return {
      creator: isSet(object.creator) ? globalThis.String(object.creator) : "",
      agentAddress: isSet(object.agentAddress)
        ? globalThis.String(object.agentAddress)
        : isSet(object.agent_address)
        ? globalThis.String(object.agent_address)
        : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: MsgEndorseAgent): unknown {
    const obj: any = {};
    if (message.creator !== "") {
      obj.creator = message.creator;
    }
    if (message.agentAddress !== "") {
      obj.agentAddress = message.agentAddress;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgEndorseAgent>): MsgEndorseAgent {
    return MsgEndorseAgent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgEndorseAgent>): MsgEndorseAgent {
    const message = createBaseMsgEndorseAgent();
    message.creator = object.creator ?? "";
    message.agentAddress = object.agentAddress ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseMsgEndorseAgentResponse(): MsgEndorseAgentResponse {
  return { endorsementId: "0" };
}

export const MsgEndorseAgentResponse: MessageFns<MsgEndorseAgentResponse> = {
  encode(message: MsgEndorseAgentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endorsementId !== "0") {
      writer.uint32(8).uint64(message.endorsementId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgEndorseAgentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgEndorseAgentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.endorsementId = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgEndorseAgentResponse {
    return {
      endorsementId: isSet(object.endorsementId)
        ? globalThis.String(object.endorsementId)
        : isSet(object.endorsement_id)
        ? globalThis.String(object.endorsement_id)
        : "0",
    };
  },

  toJSON(message: MsgEndorseAgentResponse): unknown {
    const obj: any = {};
    if (message.endorsementId !== "0") {
      obj.endorsementId = message.endorsementId;
    }
    return obj;
  },

  create(base?: DeepPartial<MsgEndorseAgentResponse>): MsgEndorseAgentResponse {
    return MsgEndorseAgentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MsgEndorseAgentResponse>): MsgEndorseAgentResponse {
    const message = createBaseMsgEndorseAgentResponse();
    message.endorsementId = object.endorsementId ?? "0";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
