// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v6.33.4
// source: clawchain/messaging/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params.js";

export const protobufPackage = "clawchain.messaging.v1";

/** QueryParamsRequest is request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params holds all the parameters of this module. */
  params: Params | undefined;
}

/** QueryMessagesRequest defines the request for querying messages. */
export interface QueryMessagesRequest {
  address: string;
}

/** QueryMessagesResponse defines the response for querying messages. */
export interface QueryMessagesResponse {
  messages: MessageEntry[];
}

/** QueryConversationRequest defines the request for querying a conversation. */
export interface QueryConversationRequest {
  addressA: string;
  addressB: string;
}

/** QueryConversationResponse defines the response for querying a conversation. */
export interface QueryConversationResponse {
  messages: MessageEntry[];
}

/** MessageEntry represents a single on-chain message. */
export interface MessageEntry {
  id: string;
  sender: string;
  recipient: string;
  ciphertext: string;
  nonce: string;
  blockHeight: string;
  timestamp: string;
  acknowledged: boolean;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<QueryParamsRequest>): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create(base?: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryParamsResponse>): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryMessagesRequest(): QueryMessagesRequest {
  return { address: "" };
}

export const QueryMessagesRequest: MessageFns<QueryMessagesRequest> = {
  encode(message: QueryMessagesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMessagesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMessagesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.address = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMessagesRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryMessagesRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMessagesRequest>): QueryMessagesRequest {
    return QueryMessagesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMessagesRequest>): QueryMessagesRequest {
    const message = createBaseQueryMessagesRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryMessagesResponse(): QueryMessagesResponse {
  return { messages: [] };
}

export const QueryMessagesResponse: MessageFns<QueryMessagesResponse> = {
  encode(message: QueryMessagesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      MessageEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryMessagesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryMessagesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messages.push(MessageEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryMessagesResponse {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => MessageEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryMessagesResponse): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => MessageEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryMessagesResponse>): QueryMessagesResponse {
    return QueryMessagesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryMessagesResponse>): QueryMessagesResponse {
    const message = createBaseQueryMessagesResponse();
    message.messages = object.messages?.map((e) => MessageEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseQueryConversationRequest(): QueryConversationRequest {
  return { addressA: "", addressB: "" };
}

export const QueryConversationRequest: MessageFns<QueryConversationRequest> = {
  encode(message: QueryConversationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.addressA !== "") {
      writer.uint32(10).string(message.addressA);
    }
    if (message.addressB !== "") {
      writer.uint32(18).string(message.addressB);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryConversationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryConversationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.addressA = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.addressB = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryConversationRequest {
    return {
      addressA: isSet(object.addressA)
        ? globalThis.String(object.addressA)
        : isSet(object.address_a)
        ? globalThis.String(object.address_a)
        : "",
      addressB: isSet(object.addressB)
        ? globalThis.String(object.addressB)
        : isSet(object.address_b)
        ? globalThis.String(object.address_b)
        : "",
    };
  },

  toJSON(message: QueryConversationRequest): unknown {
    const obj: any = {};
    if (message.addressA !== "") {
      obj.addressA = message.addressA;
    }
    if (message.addressB !== "") {
      obj.addressB = message.addressB;
    }
    return obj;
  },

  create(base?: DeepPartial<QueryConversationRequest>): QueryConversationRequest {
    return QueryConversationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryConversationRequest>): QueryConversationRequest {
    const message = createBaseQueryConversationRequest();
    message.addressA = object.addressA ?? "";
    message.addressB = object.addressB ?? "";
    return message;
  },
};

function createBaseQueryConversationResponse(): QueryConversationResponse {
  return { messages: [] };
}

export const QueryConversationResponse: MessageFns<QueryConversationResponse> = {
  encode(message: QueryConversationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      MessageEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryConversationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryConversationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messages.push(MessageEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryConversationResponse {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => MessageEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: QueryConversationResponse): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => MessageEntry.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<QueryConversationResponse>): QueryConversationResponse {
    return QueryConversationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<QueryConversationResponse>): QueryConversationResponse {
    const message = createBaseQueryConversationResponse();
    message.messages = object.messages?.map((e) => MessageEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessageEntry(): MessageEntry {
  return {
    id: "0",
    sender: "",
    recipient: "",
    ciphertext: "",
    nonce: "",
    blockHeight: "0",
    timestamp: "0",
    acknowledged: false,
  };
}

export const MessageEntry: MessageFns<MessageEntry> = {
  encode(message: MessageEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "0") {
      writer.uint32(8).uint64(message.id);
    }
    if (message.sender !== "") {
      writer.uint32(18).string(message.sender);
    }
    if (message.recipient !== "") {
      writer.uint32(26).string(message.recipient);
    }
    if (message.ciphertext !== "") {
      writer.uint32(34).string(message.ciphertext);
    }
    if (message.nonce !== "") {
      writer.uint32(42).string(message.nonce);
    }
    if (message.blockHeight !== "0") {
      writer.uint32(48).int64(message.blockHeight);
    }
    if (message.timestamp !== "0") {
      writer.uint32(56).int64(message.timestamp);
    }
    if (message.acknowledged !== false) {
      writer.uint32(64).bool(message.acknowledged);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMessageEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sender = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recipient = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ciphertext = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.nonce = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.blockHeight = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.timestamp = reader.int64().toString();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.acknowledged = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MessageEntry {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "0",
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
      recipient: isSet(object.recipient) ? globalThis.String(object.recipient) : "",
      ciphertext: isSet(object.ciphertext) ? globalThis.String(object.ciphertext) : "",
      nonce: isSet(object.nonce) ? globalThis.String(object.nonce) : "",
      blockHeight: isSet(object.blockHeight)
        ? globalThis.String(object.blockHeight)
        : isSet(object.block_height)
        ? globalThis.String(object.block_height)
        : "0",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "0",
      acknowledged: isSet(object.acknowledged) ? globalThis.Boolean(object.acknowledged) : false,
    };
  },

  toJSON(message: MessageEntry): unknown {
    const obj: any = {};
    if (message.id !== "0") {
      obj.id = message.id;
    }
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    if (message.recipient !== "") {
      obj.recipient = message.recipient;
    }
    if (message.ciphertext !== "") {
      obj.ciphertext = message.ciphertext;
    }
    if (message.nonce !== "") {
      obj.nonce = message.nonce;
    }
    if (message.blockHeight !== "0") {
      obj.blockHeight = message.blockHeight;
    }
    if (message.timestamp !== "0") {
      obj.timestamp = message.timestamp;
    }
    if (message.acknowledged !== false) {
      obj.acknowledged = message.acknowledged;
    }
    return obj;
  },

  create(base?: DeepPartial<MessageEntry>): MessageEntry {
    return MessageEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MessageEntry>): MessageEntry {
    const message = createBaseMessageEntry();
    message.id = object.id ?? "0";
    message.sender = object.sender ?? "";
    message.recipient = object.recipient ?? "";
    message.ciphertext = object.ciphertext ?? "";
    message.nonce = object.nonce ?? "";
    message.blockHeight = object.blockHeight ?? "0";
    message.timestamp = object.timestamp ?? "0";
    message.acknowledged = object.acknowledged ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
