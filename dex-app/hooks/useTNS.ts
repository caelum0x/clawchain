/**
 * TNS (Terra Name Service) is not available on ClawChain.
 * This stub returns null for all addresses.
 */
const useTNS = (_address?: string): string | null => {
  return null;
};

export default useTNS;
